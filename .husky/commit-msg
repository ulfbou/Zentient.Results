#!/bin/sh
# This file is currently inactive as linting steps are commented out in ci.yml.
# To activate local commit message linting, ensure 'npm install' and 'npx husky install' are run,
# and uncomment the relevant linting steps in ci.yml.

. "$(dirname "$0")/_/husky.sh"

# npx --no-install commitlint --edit "$1"
